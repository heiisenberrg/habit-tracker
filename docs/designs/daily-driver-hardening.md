---
status: ACTIVE
---
# CEO Plan: Routiner Daily-Driver Hardening
Generated by /plan-ceo-review on 2026-08-30
Branch: main | Mode: SELECTIVE EXPANSION
Repo: git@github.com:heiisenberrg/habit-tracker.git (remote added + full tree landed in sliced commits during this review, per user instruction)

## Vision

### 10x Check
The 2026 market splits habit trackers (Streaks, Habitica) from app blockers
(Opal, One Sec): trackers ignore phone use, blockers ignore habits. Routiner's
habit-gated Screen Time shield — finish Meditate to unlock Instagram — is the
bridge the category lacks, and it already works. The 10x version makes that
loop unmissable at every surface: the block screen itself coaches ("12/30 min
to unlock"), the evening nudge arrives while the day is still winnable, one
chat line logs a habit, and the streak survives a sick day. Everything else
(sync, TestFlight) is plumbing an audience of one doesn't need yet.

## Scope Decisions

| # | Proposal | Effort | Decision | Reasoning |
|---|----------|--------|----------|-----------|
| D4 | Plan shape: B Daily-driver hardening (vs A land-only, C cathedral) | M | ACCEPTED | Durability + device build make it real; CloudKit is heavy for one user |
| D5 | Review mode | — | SELECTIVE EXPANSION | Baseline rigor + cherry-picked upgrades |
| D6 | Streak freeze (earned insurance, absorbs one missed day) | S | ACCEPTED | Cheapest high-delight; protects the metric the app orbits |
| D7 | Assistant quick-log ("done meditate", "500 ml") | S-M | ACCEPTED | Turns assistant into a daily driver; needs a NEW intent parser (parseQuickAdd only handles creation syntax) |
| D8 | Custom shield screen (ShieldConfiguration extension) | M | ACCEPTED | User overrode defer — crown-jewel polish lands with plan B |
| D9 | Evening recap notification (~21:00, skip perfect days) | S | ACCEPTED | Cheapest retention mechanic; honors Zen/vacation |
| D10 | Widget lock-status upgrade + accessory family | M | DEFERRED | Stacks cleanly after shield screen proves the loop |
| 1A | One consolidated App Group payload (widget + shield read same `sharedState`) | S | ACCEPTED | Single writer prevents widget/shield drift |
| 2A | HomeScreen split into HeroBanner/ZenControls/TasksSection/HabitsSection | S | ACCEPTED | Stops the god-screen before freeze/recap UI lands |
| 3A | Real day-rollover into `histories` (P1 prerequisite for D6) | M | ACCEPTED | Streak can't honestly grow or break without it (reviewer C1) |
| 4A | Backup import/restore in Settings | S | ACCEPTED | Export without import is half a durability story (reviewer C2) |
| 5C | Backup written natively to App Group container on background | S | REVERSED (8A) | Outside voice: App Group dies with uninstall; JS second-key backup is equal coverage with zero Swift |
| 8A | Auto-backup + corrupt-dump both live in a second AsyncStorage key; Export/Import is the off-device story | S | ACCEPTED | Simpler mechanism, same threat coverage |
| 9C | Keep D7 quick-log AND extend 2A split to AssistantScreen | S | ACCEPTED | User taste keeps chat logging; god-screen standard applied consistently |
| 10A | Full seed honesty: empty initial() completions, zero synthetic days at reconciliation, streak restarts real | M | ACCEPTED | Reset must never unshield; the flame must be true (one-time visible streak drop) |
| 11B | Android theming pass | S | DEFERRED | Audience-of-zero polish; moved to TODOS Android section |
| OV1 | All store mutations flow through the store API after hydration (no raw snapshot writes anywhere) | M | ACCEPTED | Outside voice: cited write pattern doesn't exist; raw writes get clobbered by zustand persist |
| OV2 | Persisted streak counter {current, best} maintained at rollover; freeze/vacation applied at roll time | M | ACCEPTED | Fixes freeze-invisible-to-math AND the structural 84-day streak cap |
| OV3 | D8 starts with a provisioning spike (Family Controls on new extension id, iOS 16 floor); refusal auto-defers D8 | S | ACCEPTED | Entitlement grant under the borrowed team is unverified |

## Accepted Scope (added to this plan)

### Ordering constraint (reviewer C6/C9)
1. Migration hardening (v3) lands FIRST — every persisted field added by this
   plan rides that migration. 2. Day rollover (3A). 3. 1A consolidated payload
   BEFORE the D8 shield extension (the shield copy reads it; until then the
   generic fallback is the accepted interim). 4. Other features in any order.
5. The device Release build lands LAST, after the shield extension changes the
   Xcode project (otherwise it builds twice).

### Baseline (plan B)
- Land the work: DONE — pushed to git@github.com:heiisenberrg/habit-tracker.git
  as 9 sliced commits during this review.
- **Store migration hardening (reviewer C9):** bump persist `version` to 3 with
  real per-version `migrate` steps. v2→v3 adds every persisted field this plan
  introduces: `streakFreezes {available, usedOn, runLength}`,
  `streak {current, best}`, `lastRolledDay`, `historyReconciled` — defaults
  frozen here: freezes 0/[]/0, streak 0/0, lastRolledDay = install day,
  historyReconciled false (recap carries NO prefs — it is fully automatic).
  `migrate` must never return `initial()`. Corrupt-snapshot capture happens
  INSIDE the custom `storage.getItem` wrapper (that is where JSON.parse runs):
  save the raw string to the single `routiner-corrupt-dump` AsyncStorage key
  (overwrite in place), then return null so zustand hydrates defaults and
  hydration SUCCEEDS — the deadlocking rehydrate-catch path never fires.
  Backup import runs old files through these same migrate steps (a v2 backup
  imports into a v3 app losslessly). Fixture test: a frozen v2 JSON snapshot
  must migrate to v3 with zero data loss (deep-equal on every v2 field; new
  v3 fields equal their documented defaults).
- **Day rollover (3A — P1 prerequisite, reviewer C1):** on first store access of
  a new day (app open or background fetch), iterate EVERY elapsed day since
  `lastRolledDay` (capped at 83, the histories window size): write that day's
  real completion fractions
  into `histories` (zero-days for days with no data), shift the window one day
  per iteration, and apply freeze consumption per elapsed day (D6 rules). The
  `vacationMode` flag is evaluated as currently set for all elapsed days —
  stated simplification. One-time reconciliation (guarded by
  `historyReconciled`) replaces synthetic tail days with real data from the
  `completions` map where it exists; reconciled history seeds freeze
  `runLength` at 0 (no retroactive accrual of any kind). Unit-tested with
  clock fixtures incl. the 7-days-offline case; affects dayStreak,
  perfectDayCount, karma, widget dots, Activity heatmap.
- **Backup & restore (4A revised by 8A):** on app-background, the full
  versioned store export is mirrored to a second AsyncStorage key (pure JS —
  both platforms); the corrupt-rehydrate dump goes to another AsyncStorage key
  the same way (parse failure ≠ storage failure, so no native write needed).
  Settings gains Export (share sheet — the only true off-device durability)
  and Import (validate version → run the same migrate steps → confirm gate →
  pre-import safety snapshot → replace store). Round-trip unit test:
  export → import → deep-equal.
- **Single-writer rule (OV1, outside voice):** every mutation — notification
  actions, background rollover, imports — goes through the zustand store API
  after awaiting hydration (`persist.onFinishHydration`); NO code path writes
  the `routiner-store` key directly. Headless contexts import the store,
  await hydration, call actions. Day rollover is triggered by three explicit
  hooks: store hydration completion, AppState → active, and a foreground
  midnight timer (the 23:55-crossing case).
- **Streak counter (OV2, outside voice):** rollover maintains persisted
  `streak { current, best }`; a rolled perfect day increments, a missed day
  consumes a freeze or resets, vacation days no-op (evaluated with the flag as
  currently set — stated simplification). `dayStreak` reads the counter (no
  84-day cap); `histories` stays pure chart data. Fraction semantics: histories
  hold 0..1 fractions and EVERY consumer thresholds at >= 1 for "done" —
  including `habitStreak` (currently truthy-buggy on fractions), `totalOf`,
  `rate30`, HabitDetail and Profile screens.
- **Notification "Mark as done" actions (reviewer C4 + OV):** per-habit
  notification categories, re-registered whenever habits change, with existing
  triggers rescheduled to carry their categoryId. Check habits: one "Mark done"
  action. Count habits: two actions — "+step" (e.g. "+500 ML") and "Complete".
  Handler follows the single-writer rule (OV1), then re-evaluates App Lock +
  shared payload.
- **Seed honesty (10A):** `initial()` seeds EMPTY completions (a fresh or
  reset store must never satisfy App Lock — Reset re-evaluates the shield
  immediately); one-time reconciliation ZEROES pre-install synthetic days;
  the streak counter starts from the user's real consecutive run. One-time
  in-app note explains the honest streak drop after upgrade.
- Widget avatar asset swap (needs user-supplied image file); scrub leftover
  social demo copy — known surfaces: `src/data/profileFeed.ts` feed strings,
  Notifications seed items ("Your friends miss you"), `src/data/seed.ts`
  friends fixtures (reviewer C10).

### Expansions (accepted this review)
- **Streak freeze (D6) — rules (reviewer C3):** accrue 1 freeze per 7
  *consecutive* perfect days, counter resets on an imperfect day; cap 2 held.
  Consumption happens at day rollover: each missed day consumes exactly one
  freeze if available, else the streak resets (2 missed days with 1 freeze =
  reset). Vacation-mode days neither break the streak nor consume freezes nor
  count toward accrual. No retroactive accrual from synthetic history. State
  `streakFreezes: { available: number; usedOn: string[]; runLength: number }`
  rides the v3 migration.
- **Assistant quick-log (D7, reviewer C5):** a NEW intent parser (not
  parseQuickAdd, which only parses creation syntax) — fuzzy habit-name match +
  quantity/unit parse for phrases like "done meditate" / "500 ml" /
  "log 20 min reading". Ambiguity → the flow asks "which habit?" with chips;
  no match → offers to create. Effort honestly S-M because the parser is new.
- **Shield screen (D8) — implementation notes (reviewer C8 + OV3):** STEP ONE
  is a provisioning spike — create the Managed Settings UI extension target
  (xcodeproj-ruby recipe) with `com.apple.developer.family-controls` on its
  new bundle id and verify the borrowed team (QAW658347B) mints a profile;
  refusal auto-defers D8 to TODOS with no sunk feature work. iOS 16 minimum
  stated. Then: `ShieldConfiguration` data source joins App Group
  `group.com.lucidbots.lucidbots`; UI limited to title/subtitle/icon/buttons;
  refreshes when re-presented, NOT live. The app mirrors unlock-habit name +
  progress into the consolidated `sharedState` payload (1A) on every
  completion write. Missing/corrupt payload → generic fallback copy, never a
  crash.
- **Evening recap (D9) — trigger lifecycle (reviewer C7):** a ONE-SHOT 21:00
  Notifee trigger (not repeating — a repeating trigger would fire stale
  summaries on days the app never runs; a missed recap is the deliberately
  chosen failure mode). Recomputed and rescheduled for the next 21:00 on every
  completion change while the app runs and by the existing background-fetch
  path; cancelled when the day turns perfect, when a Zen session covers 21:00,
  or when vacation mode is on; re-armed at next evaluation. Suppression = the
  trigger simply isn't scheduled. No user-facing preferences in v1.

### Eng-review hardening (2026-08-30, /plan-eng-review; corrected by eng outside voice)
- **E1 (headless invariant — documented, NOT guarded):** react-native-
  background-fetch dispatches headless work into the EXISTING runtime when the
  process is alive (HeadlessTask.java silently no-ops when RESUMED); a second
  store instance exists only when the process is dead. Invariant documented
  beside OV1; no heartbeat guard (a guard could only misfire, blocking the
  post-force-kill rollover).
- **E2 (two canonical selectors):** `dayCompletion(dateKey)` → 0..1 habit
  fraction (charts/history), and `dayPerfect(dateKey)` → boolean including
  PLANNER TASKS for that date (the original "streaks count habits AND tasks"
  rule). Rollover feeds the counter from `dayPerfect` and histories from
  `dayCompletion`; `perfectToday === dayPerfect(todayKey())` by construction.
  All prior consumers route through these two.
- **E3 (test additions):** recap content-builder suite (perfect/1-to-go/zen/
  vacation/locked variants) and notification-action semantics suite (check ×
  count habits × both actions × foreground/background delivery).
- **E4 (storage error channel):** dump and auto-mirror each keep exactly ONE
  latest copy. Write-failure logging lives in a custom `storage.setItem`
  wrapper (persist discards setItem rejections — onRehydrateStorage never
  sees them); quota failures log loudly and surface as a Settings staleness
  timestamp.

### Eng outside-voice corrections (2026-08-30 — supersede any conflicting wording above)
1. **Hydration gate:** every non-UI mutation path checks
   `persist.hasHydrated()` first and only then awaits `onFinishHydration`;
   with the getItem wrapper, corrupt stores still hydrate (to defaults), so
   the gate always resolves.
2. **Import mechanism:** a dedicated `importState` store action replaces each
   persisted field explicitly — never `setState(imported, true)` (which would
   strip every action) and never a blind merge.
3. **Batched rollover:** the entire multi-day roll (histories shift + counter
   + freezes + lastRolledDay) commits in ONE `set()` — not one write per day.
4. **Day math:** iterate calendar dateKeys via `addDays` on local dates (no
   ms/86400000 division, DST-safe); `lastRolledDay >= today` → no-op (clock
   regression). Fix the existing UTC-parse precedent in
   `src/services/widget.ts` (new Date(dateKey) parses UTC) as part of T3.
5. **Actions on Android:** actions are embedded per-notification (categories
   are iOS-only) — habit changes rebuild the affected trigger notifications
   on both platforms via one path. Register `notifee.onForegroundEvent`
   (currently absent) so foreground taps aren't dropped.
6. **"+step" lifecycle:** after a +step press the handler re-posts an updated
   notification ("1500 ML to go" + same actions); "Complete" or goal-reached
   cancels it.
7. **afterMutation() seam:** one shared post-write hook (App Lock re-eval +
   sharedState payload + recap re-arm) called by notification handlers,
   quick-log, import, and rollover — the App.tsx effects remain the UI-path
   caller.
8. **Ordering:** C4 notification actions ALSO depend on 1A (payload writes);
   move it after 1A alongside D8. Drop vestigial "backup metadata" from the
   1A payload (post-8A nothing native reads it).
9. **Shield v1 scope:** ShieldConfiguration supplies labels/icon ONLY —
   custom button behavior would need a separate ShieldAction extension and
   still could not open Routiner; v1 promises no buttons.
10. **Widget reload budget:** the bridge call always writes App Group
    defaults; `reloadAllTimelines()` is debounced (≥60s, immediate only on
    lock-state flips) to respect WidgetKit's daily reload budget.
11. **Android recap timing:** Doze may defer the 21:00 one-shot on Android;
    accepted (iOS is the daily driver) and documented — no
    SCHEDULE_EXACT_ALARM permission requested.

### Architecture decisions (this review)
- **1A:** one consolidated App Group `sharedState` JSON (streak, week marks,
  lock state, unlock habit, progress, backup metadata) written by a single
  bridge call; widget Swift decode migrates to the new shape in the same change.
- **2A (extended by 9C):** HomeScreen splits into HeroBanner, ZenControls,
  TasksSection, HabitsSection; AssistantScreen (the file actually gaining
  features) splits into ChatTranscript, FlowInput, MenuCards. Mechanical
  refactors, screenshot-verified, no behavior change.

## NOT in scope (explicitly rejected/reserved)
- CloudKit/live multi-device sync (plan C)
- Own bundle id + TestFlight distribution (plan C; bundle-id change resets device data)
- Extra widget families / shareable streak-card image (lower-priority candidates, unrequested)

## Deferred to TODOS.md
- D10 Widget lock-status upgrade (🔒 line + today ring + lock-screen accessory widget) — after shield screen lands
- Shareable streak-card image export
- Android: Google Fit steps, widget parity, AppCompatDelegate dark mode + theming pass (11B)

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 1 | CLEAN | 5 proposals, 4 accepted, 1 deferred |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 2 | CLEAN | 2026-08-31 (post-QA diff): 32 issues, 0 critical gaps · 2026-08-30 (plan): 20 issues, 0 critical gaps |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | — |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

- **CROSS-MODEL:** three Claude outside voices (fresh-context) have run — CEO-stage (13), eng-stage on the plan (16), and eng-stage on the 2026-08-31 simulator-QA fix diff (10 findings: 2 agreed with the review, 6 tensions decided by the user — 5 accepted, 1 kept as reviewed — plus 4 hygiene corrections accepted). Zero auto-incorporated.
- **VERDICT:** CEO + ENG CLEARED — implement tasks T1–T14 (`~/.gstack/projects/habittracker/tasks-eng-review-20260831-094912.jsonl`), then re-run the five Maestro flows green before committing.

NO UNRESOLVED DECISIONS
