<?xml version="1.0" encoding="utf-8"?>
<!-- Slay Streak. Defaults to 2x2 (the compact face) and grows to the
     letters+lock face at ≥200dp wide; minResize keeps two rows so the dots
     never get squeezed into a 4x1 strip. 30 min is the platform minimum
     period; the app repaints on every mutation anyway. The API-31+ attributes
     (targetCell*, previewLayout, description) are ignored by older hosts. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_streak_description"
    android:initialLayout="@layout/widget_streak_small"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="110dp"
    android:previewLayout="@layout/widget_streak_medium"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:updatePeriodMillis="1800000"
    android:widgetCategory="home_screen" />
