<?xml version="1.0" encoding="utf-8"?>
<!-- Quote of the day. Defaults to a 4x1 strip and resizes up to 4x2, where
     the eyebrow header appears. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_quote_description"
    android:initialLayout="@layout/widget_quote"
    android:minWidth="250dp"
    android:minHeight="40dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="40dp"
    android:previewLayout="@layout/widget_quote"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:updatePeriodMillis="1800000"
    android:widgetCategory="home_screen" />
